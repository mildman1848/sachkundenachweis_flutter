name: sachkundenachweis
description: Vorbereitung auf den Sachkundenachweis nach LHundG NRW
version: 1.0.0+1
publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"  # Dart 3.5 wird von Flutter 3.22.1 unterstützt
  flutter: ">=3.32.4"

dependencies:
  flutter:
    sdk: flutter

  # Lokale Speicherung
  shared_preferences: ^2.2.2

  # Fortschrittsanzeige (Diagramme)
  fl_chart: ^1.0.0

  # Icons im Cupertino-Stil (optional)
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linter-Regeln für Dart 3.5+
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
